# 第一章 SQL语句
## DDL
 alter table student add/modify/change/drop/rename
[[ddl语句总结.png]]
注意：
1. 均可以多行，比如add s_age int,add s_name varchar(50);(modify,change,drop同理)
2. FIRST  和AFTER 字段名可以用来控制字段位置
3. DDL语句还有create table，记住是小括号
DML，DQL，DCL

